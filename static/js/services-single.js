// Example JavaScript code. You could enhance this script to periodically
// refresh the table data or trigger additional PowerShell remoting calls
// via AJAX endpoints on the server if needed.

document.addEventListener("DOMContentLoaded", function() {
    console.log("Services Single JS loaded.");
    // You can add further logic to interact with the table,
    // e.g., making additional AJAX calls to a new Flask route to update service statuses.
});
